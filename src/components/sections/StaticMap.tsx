"use client";

import { useMemo, useState } from "react";
import { MarkerCard } from "@/components/cards/MarkerCard";
import { mapCategories, mapMarkers } from "@/data/map";
import type { MapCategory } from "@/types";
import { cn } from "@/lib/utils";

const colors: Record<string, string> = { 의료치유: "bg-[#E53935]", 돌봄: "bg-[#0072CE]", 골목상권: "bg-[#FFD84D]", 정주환경: "bg-[#0F766E]", 교육: "bg-[#004EA2]", 친수공간: "bg-[#0F766E]", 교통기반: "bg-[#082A5A]", 역사문화: "bg-[#8B5CF6]" };
export function StaticMap({ preview = false }: { preview?: boolean }) {
  const [category, setCategory] = useState<MapCategory>("전체");
  const [selected, setSelected] = useState(mapMarkers[0]);
  const markers = useMemo(() => category === "전체" ? mapMarkers : mapMarkers.filter((m) => m.category === category), [category]);
  const shown = preview ? markers.slice(0, 8) : markers;
  return <div className="grid gap-6 lg:grid-cols-[1.05fr_.95fr]"><div className="surface-panel rounded-[2rem] p-5 md:p-7"><div className="mb-5 flex flex-wrap gap-2">{mapCategories.map((item) => <button key={item} type="button" onClick={() => { setCategory(item); const next = item === "전체" ? mapMarkers : mapMarkers.filter((m) => m.category === item); setSelected(next[0] || mapMarkers[0]); }} aria-pressed={category === item} className={cn("rounded-full border px-3 py-2 text-sm font-black", category === item ? "border-[#004EA2] bg-[#004EA2] text-white" : "border-[#E5E7EB] bg-white text-[#344054]")}>{item}</button>)}</div><div className="relative aspect-[16/11] overflow-hidden rounded-[1.5rem] border border-[#D7E4FF] bg-[#F7FAFF] md:aspect-[16/10]"><svg viewBox="0 0 760 460" className="absolute inset-0 h-full w-full" role="img" aria-label="태전2동, 구암동, 국우동·도남권을 도형으로 구분한 북구 공약지도"><rect width="760" height="460" fill="#F7FAFF"/><circle cx="96" cy="80" r="118" fill="#FFD84D" opacity=".16"/><circle cx="650" cy="380" r="130" fill="#0072CE" opacity=".10"/><path d="M58 132C126 88 210 98 262 154C314 210 258 292 178 304C92 316 28 246 42 176C46 156 48 146 58 132Z" fill="#EAF4FF" stroke="#8FB7F1" strokeWidth="3"/><path d="M286 92C370 60 478 96 520 176C556 244 498 320 404 318C310 316 246 246 250 168C252 128 260 104 286 92Z" fill="#FFF7D6" stroke="#F0CC4F" strokeWidth="3"/><path d="M534 128C606 78 706 112 728 198C748 278 672 350 586 324C510 302 480 226 506 166C514 148 522 136 534 128Z" fill="#E8F6F1" stroke="#8ACAB8" strokeWidth="3"/><path d="M72 338C190 304 296 318 400 302C516 284 614 258 724 278" stroke="#2FBF71" strokeWidth="18" strokeLinecap="round" opacity=".38"/></svg>{shown.map((marker) => <button key={marker.title} type="button" onClick={() => setSelected(marker)} className="map-marker absolute -translate-x-1/2 -translate-y-1/2" style={{ left: `${marker.x}%`, top: `${marker.y}%` }} aria-label={`${marker.title} 공약 보기`} title={marker.title}><span className={cn("flex h-7 w-7 items-center justify-center rounded-full border-4 shadow-lg md:h-8 md:w-8", selected.title === marker.title ? "scale-125 border-[#FFD84D] ring-4 ring-white/80" : "border-white", colors[marker.category], marker.category === "골목상권" ? "text-[#082A5A]" : "text-white")}><span className="h-2 w-2 rounded-full bg-current" /></span></button>)}<div className="pointer-events-none absolute inset-x-3 bottom-3 grid grid-cols-3 gap-2 md:hidden"><span className="rounded-xl bg-white/86 px-2 py-1 text-center text-xs font-black text-[#082A5A]">태전2동</span><span className="rounded-xl bg-white/86 px-2 py-1 text-center text-xs font-black text-[#082A5A]">구암동</span><span className="rounded-xl bg-white/86 px-2 py-1 text-center text-xs font-black text-[#082A5A]">국우·도남</span></div></div><div className="mt-4 rounded-2xl border border-[#D7E4FF] bg-white p-5"><p className="text-xs font-black text-[#004EA2]">{selected.category} · {selected.area}</p><h3 className="mt-2 text-xl font-black text-[#082A5A]">{selected.title}</h3><p className="mt-2 text-sm leading-6 text-[#667085]">{selected.issue}</p><p className="mt-2 text-sm font-bold leading-6 text-[#344054]">{selected.pledge}</p></div></div><div className="grid max-h-[620px] gap-3 overflow-auto pr-1">{shown.map((marker) => <MarkerCard key={marker.title} marker={marker} />)}</div></div>;
}
